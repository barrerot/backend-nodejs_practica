<%- include('head', {title: title}) %>

<body>
<%- include('header', {title: title})%>

    <div class="container">
        <nav class="migas">
            <h3>Productos en Venta</h3>
        </nav>
        <div class="item-columns">
            <% anunciosResult.forEach(function(anuncio){%>
                <% if(anuncio.venta) { %>
                    <div class="item bg-light border-secondary">
                        <%- include('./anuncio_info', {anuncio:anuncio}) %>
                    </div>
                <% }; %>
            <%})%>
        </div>

        <nav class="migas">
            <h3>Productos en Búsqueda</h3>
        </nav>
        <div class="item-columns">
            <% anunciosResult.forEach(function(anuncio){%>
                <% if(!anuncio.venta) { %>
                    <div class="item bg-light border-secondary">
                        <%- include('./anuncio', {anuncio:anuncio}) %>
                    </div>
                <% }; %>
            <%})%>
        </div>
    </div>




    <%- include('footer') %>

</body>

</html>